{
  "private": true,
  "name": "qq-download-link-extractor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "build": "esbuild index.js --bundle --minify --platform=node --sourcemap --outfile=dist/index.js",
    "dev": "esbuild index.js --bundle --platform=node --sourcemap --outfile=dist/index.js"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.6.0",
    "node-fetch": "^3.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.13.14",
    "eslint": "^8.2.0"
  }
}
